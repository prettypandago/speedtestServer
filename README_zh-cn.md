# speedtestServer - 一个轻量级的网络速度测试服务器 | 简单但强大

[![License](https://img.shields.io/badge/license-MIT-blue.svg)](LICENSE)
![Python 3.6+](https://img.shields.io/badge/python-3.6+-blue)

一个轻量级的网络速度测试服务器，提供简洁的Web界面用于测试网络下载、上传和延迟。

**语言：** [English](README.md "English version"), [简体中文](README_zh-cn.md "简体中文版")

## ✨ 功能特性

- 🚀 **下载测试** - 测试网络下载速度
- 📤 **上传测试** - 测试网络上传速度  
- 🔔 **延迟测试** - 测试网络延迟（Ping）
- 📊 **实时图表** - 基于 Chart.js 的实时速度测试结果展示
- 🎨 **简洁界面** - 现代化设计的轻量级前端
- 🔄 **跨域支持** - 内置 CORS 中间件支持跨域请求

## 📋 要求

- Python 3.6+
- Flask
- Flask-CORS

## 🚀 快速开始

### 1. 克隆项目
```bash
git clone https://github.com/prettypandago/speedtestServer.git
cd speedtestServer
```

### 2. 安装依赖
```bash
pip install -r requirements.txt
```

### 3. 运行服务器
```bash
python speedtestServer.py
```

服务器将在 `http://localhost:5000` 启动

### 4. 使用测试

在浏览器中打开 `http://localhost:5000`，即可使用Web界面进行网络速度测试。

## 📁 项目结构

```
speedtestServer/
├── speedtestServer.py      # Python 后端服务器
├── requirements.txt         # Python 依赖项
├── templates/
│   ├── index.html          # Web 前端    interface
│   └── chart.min.js        # Chart.js libraryscript
├── LICENSE                 # MIT 许可证
├── README.md               # 英语版本
└── README_zh-cn.md         # 本文件
```

## 🔌 API 端点

| 端点 | 方法 | 说明 |
|------|------|------|
| `/` | GET | 返回前端 HTML 页面 |
| `/ping` | GET | 连接测试（返回 "pong"） |
| `/download` | GET | 下载测试（返回大数据流） |
| `/upload` | POST | 上传测试（接收大文件数据） |

## ⚙️ 配置说明

编辑 `speedtestServer.py` 中以下配置可自定义：

```python
app.run(host='0.0.0.0', port=5000, threaded=True, debug=False)
```

- `host`: 服务器绑定地址（0.0.0.0 表示监听所有网卡）
- `port`: 服务器端口号（默认 5000）
- `threaded`: 是否启用多线程（处理并发请求）
- `debug`: 调试模式开关

## 📝 注意事项

⚠️ **项目仍在开发中**，API 和功能可能会发生变化。生产环境使用前请充分测试。

## 🤝 如何贡献

欢迎提交 Pull Request 和 Issue！

## 📜 许可证

本项目采用 [MIT 许可证](LICENSE)

---

感谢使用 speedtestServer！